#!/usr/bin/env bash
# ------------------------------------------------------------------------------
# bbr.sh — BBR congestion control management
# ------------------------------------------------------------------------------

# --- bbr_enable -------------------------------------------------------------
bbr_enable() {
    # Stub — mutating operation, must fail closed.
    error "BBR enable is not implemented."
    return 1
}

# --- bbr_status -------------------------------------------------------------
bbr_status() {
    # Stub — query stub, warn and fail.
    warn "BBR status check is not implemented."
    return 1
}
